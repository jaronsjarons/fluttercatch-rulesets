{
  "name": "@fluttercatch/rulesets",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "CC-BY-SA-4.0 AND MIT",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate": "node tools/validate.mjs",
    "build": "node tools/build.mjs",
    "check": "npm run validate && npm test && npm run build"
  }
}
